spring:
  flyway:
    locations: classpath:datebase
    table: user_flyway_schema_history
    baseline-on-migrate: true
    baseline-version: 0
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/testrest
    username: root
    password: Trh_529211346